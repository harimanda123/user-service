server:
  port: 9092

spring:
 application:
  name: USER-SERVICE
  
eureka:
 client:
  register-with-eureka: true
  fetch-registry: true
  service-url:
   defaultZone: http://localhost:9093/eureka/
 instance:
  hostname: localhost
  
department:
 service:
  endpoint: http://DEPARTMENT-SERVICE/departments/
 